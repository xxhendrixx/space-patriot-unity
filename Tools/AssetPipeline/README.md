Asset build tools
=================

Run npm install in this directory, then npm run terrain and npm run models. In Unity, run Space Patriot > Restore original modeled assets. The importer retains existing asset GUIDs.

The copied JavaScript engine/model adapters execute the preserved original source. industrial-refit.js adds the new chassis shells; original hulls and cabins remain available separately. No Unity installation is required for the Node exports.
